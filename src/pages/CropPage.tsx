import React from 'react';
import CropperComponent from '../features/crop/components/CropperComponent';

const CropPage: React.FC = () => {
  return (
    <div className="app-container">
      <CropperComponent />
    </div>
  );
};

export default CropPage;
